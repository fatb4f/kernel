# build/

This directory is **non-authoritative**.

Purpose:
- disposable local build outputs
- ephemeral toolchain residue
- scratch materialization during local development
- never a semantic source of truth

Rules:
- safe to delete and regenerate
- not an input to canonical structural authority
- not an allowed substitute for generated committed views
- build residue must never be promoted into source authority
