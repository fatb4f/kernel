# Projection Manifest Class

`pkt-r06-04` establishes `projections/` as an active target class.

No legacy projection control object survives directly from the legacy archive; the class is created here so later tranches can place target-shaped projection control objects without re-opening manifest ownership.
