# Generator Manifest Class

`pkt-r06-04` establishes `generators/` as an active target class.

No legacy generator control object survives directly from the legacy archive; the class is created here so later tranches can place target-shaped generator control objects without re-opening manifest ownership.
