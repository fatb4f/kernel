# Bundle Manifest Class

`pkt-r06-04` establishes `bundles/` as an active target class.

No active bundle control object is materialized from legacy content in this step because the only legacy bundle-like freeze is provider-specific and remains deferred.
